server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Angular usa path-based routing lato client: qualunque route
        # sconosciuta a nginx deve ricadere su index.html
        try_files $uri $uri/ /index.html;
    }
}
